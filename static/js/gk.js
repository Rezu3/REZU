 const gkQuestions = [
    {
        question: "ভারতে প্রথম সুতিবস্ত্র কল (Cotton Mill) কোথায় স্থাপিত হয়েছিল?",
        options: ["বোম্বে", "সুরাট", "কলকাতা", "কানপুর"],
        correctAnswer: 2 // ১৮১৮ সালে ঘুচড়িতে
    },
    {
        question: "ভারতে রেলপথ কত সালে চালু হয়?",
        options: ["১৮৫৩", "১৮৫০", "১৮৫৬", "১৯০১"],
        correctAnswer: 0
    },
    {
        question: "টাটা আয়রন অ্যান্ড স্টিল কোম্পানি (TISCO) কত সালে প্রতিষ্ঠিত হয়?",
        options: ["১৯০৫", "১৯০৭", "১৯১১", "১৯১৫"],
        correctAnswer: 1
    },
    {
        question: "ভারতে প্রথম পাটকল (Jute Mill) কোথায় স্থাপিত হয়েছিল?",
        options: ["হাওড়া", "রিষড়া", "হলদিয়া", "নৈহাটি"],
        correctAnswer: 1
    },
    {
        question: "কাকে 'ভারতের আধুনিক শিল্পের জনক' বলা হয়?",
        options: ["জে. এন. টাটা", "দ্বারকানাথ ঠাকুর", "ধীরুভাই আম্বানি", "জি. ডি. বিড়লা"],
        correctAnswer: 0
    },
    {
        question: "ভারতের প্রথম লৌহ ও ইস্পাত শিল্প কেন্দ্র কোনটি?",
        options: ["ভিলাই", "দুর্গাপুর", "কুলটি", "জামশেদপুর"],
        correctAnswer: 2 // বেঙ্গল আয়রন ওয়ার্কস
    },
    {
        question: "ভারতে প্রথম সিমেন্ট কারখানা কোথায় স্থাপিত হয়েছিল?",
        options: ["মাদ্রাজ", "পেশোয়ার", "কলকাতা", "মুম্বাই"],
        correctAnswer: 0
    },
    {
        question: "কোন শহরকে 'ভারতের ম্যানচেস্টার' বলা হয়?",
        options: ["কানপুর", "আমেদাবাদ", "কোয়েম্বাটুর", "মুম্বাই"],
        correctAnswer: 1
    },
    {
        question: "ভারতে প্রথম চা বাগান কোথায় শুরু হয়?",
        options: ["দার্জিলিং", "আসাম", "কেরালা", "নীলগিরি"],
        correctAnswer: 1
    },
    {
        question: "ভারতের সিলিকন ভ্যালি কোন শহরকে বলা হয়?",
        options: ["হায়দ্রাবাদ", "পুনে", "বেঙ্গালুরু", "চেন্নাই"],
        correctAnswer: 2
    },
    {
        question: "স্টিল অথরিটি অফ ইন্ডিয়া লিমিটেড (SAIL) কত সালে প্রতিষ্ঠিত হয়?",
        options: ["১৯৫৪", "১৯৭৩", "১৯৬৪", "১৯৮০"],
        correctAnswer: 1
    },
    {
        question: "ভারতের বৃহত্তম বেসরকারি শিল্প গোষ্ঠী কোনটি?",
        options: ["আদানি", "রিলায়েন্স", "টাটা", "বিড়লা"],
        correctAnswer: 1
    },
    {
        question: "বোম্বেতে প্রথম সফল সুতিবস্ত্র কল কে স্থাপন করেন?",
        options: ["কাওয়াসজি নানাভাই দাভার", "জে এন টাটা", "জামশেদজি জিজিভয়", "জি ডি বিড়লা"],
        correctAnswer: 0
    },
    {
        question: "ভারতের প্রথম পেট্রোকেমিক্যাল শিল্প কেন্দ্র কোথায় গড়ে ওঠে?",
        options: ["হলদিয়া", "জামনগর", "ট্রম্বে", "ভাদোদরা"],
        correctAnswer: 2
    },
    {
        question: "কোন পঞ্চবার্ষিকী পরিকল্পনায় ভারী শিল্পের ওপর জোর দেওয়া হয়েছিল?",
        options: ["প্রথম", "দ্বিতীয়", "তৃতীয়", "চতুর্থ"],
        correctAnswer: 1
    },
    {
        question: "ভারতের বৃহত্তম জাহাজ নির্মাণ কেন্দ্র কোনটি?",
        options: ["মাজাগাঁও ডক", "কোচিন শিপইয়ার্ড", "বিশাখাপত্তনম", "গার্ডেনরিচ"],
        correctAnswer: 1
    },
    {
        question: "কাকে 'দক্ষিণ ভারতের ম্যানচেস্টার' বলা হয়?",
        options: ["মাদুরাই", "চেন্নাই", "কোয়েম্বাটুর", "বেঙ্গালুরু"],
        correctAnswer: 2
    },
    {
        question: "ভারতে কাগজ শিল্প প্রথম কোথায় শুরু হয়?",
        options: ["শ্রীরামপুর", "লখনউ", "নেপানগর", "টিটাগড়"],
        correctAnswer: 0
    },
    {
        question: "ভারতের বৃহত্তম সার কারখানাটি কোথায় অবস্থিত?",
        options: ["ট্রম্বে", "সিন্দ্রি", "কোটা", "হলদিয়া"],
        correctAnswer: 1
    },
    {
        question: "রাউরকেল্লা ইস্পাত কেন্দ্রটি কোন দেশের সহযোগিতায় তৈরি?",
        options: ["রাশিয়া", "ব্রিটেন", "জার্মানি", "আমেরিকা"],
        correctAnswer: 2
    },
    {
        question: "ভিলাই ইস্পাত কেন্দ্রটি কোন রাজ্যে অবস্থিত?",
        options: ["ওড়িশা", "ছত্তিশগড়", "ঝাড়খণ্ড", "মধ্যপ্রদেশ"],
        correctAnswer: 1
    },
    {
        question: "ভারতের প্রথম নিউজপ্রিন্ট কারখানা কোনটি?",
        options: ["টিটাগড়", "নেপানগর", "ভদ্রাবতী", "সাহারানপুর"],
        correctAnswer: 1
    },
    {
        question: "ভারতের উদীয়মান শিল্প (Sunrise Industry) কাকে বলা হয়?",
        options: ["লোহা-ইস্পাত", "পেট্রোকেমিক্যাল", "চা শিল্প", "পাট শিল্প"],
        correctAnswer: 1
    },
    {
        question: "বোকারো ইস্পাত কেন্দ্রটি কোন দেশের সহায়তায় গড়ে উঠেছে?",
        options: ["সোভিয়েত ইউনিয়ন", "জার্মানি", "ব্রিটেন", "জাপান"],
        correctAnswer: 0
    },
    {
        question: "ভারতের বৃহত্তম অ্যালুমিনিয়াম কোম্পানি কোনটি?",
        options: ["হিন্ডালকো", "নালকো", "বালকো", "মালকো"],
        correctAnswer: 1
    },
    {
        question: "কোন শহরকে 'ভারতের বোস্টন' বলা হয়?",
        options: ["মুম্বাই", "আমেদাবাদ", "কলকাতা", "সুরাট"],
        correctAnswer: 1
    },
    {
        question: "ভারতের প্রথম অটোমোবাইল কারখানা কোথায় স্থাপিত হয়?",
        options: ["চেন্নাই", "উত্তরপাড়া", "পুনে", "গুড়গাঁও"],
        correctAnswer: 1
    },
    {
        question: "রেল ইঞ্জিন তৈরির কারখানা চিত্তরঞ্জন লোকোমোটিভ কোন রাজ্যে অবস্থিত?",
        options: ["ঝাড়খণ্ড", "বিহার", "পশ্চিমবঙ্গ", "ওড়িশা"],
        correctAnswer: 2
    },
    {
        question: "ভারতের ইলেকট্রনিক্স শহর কাকে বলা হয়?",
        options: ["হায়দ্রাবাদ", "চেন্নাই", "বেঙ্গালুরু", "গুরগাঁও"],
        correctAnswer: 2
    },
    {
        question: "ভারতের প্রথম সুগার মিল (চিনিকল) কোথায় স্থাপিত হয়েছিল?",
        options: ["উত্তরপ্রদেশ", "বিহার", "মহারাষ্ট্র", "তামিলনাড়ু"],
        correctAnswer: 1
    },
    {
        question: "ভারতের রূঢ় (Ruhr of India) কাকে বলা হয়?",
        options: ["জামশেদপুর", "দুর্গাপুর", "ভিলাই", "বোকারো"],
        correctAnswer: 1
    },
    {
        question: "ভারতের বৃহত্তম তথ্য প্রযুক্তি (IT) কোম্পানি কোনটি?",
        options: ["ইনফোসিস", "উইপ্রো", "TCS", "HCL"],
        correctAnswer: 2
    },
    {
        question: "ভারতে তৈল শোধনাগার প্রথম কোথায় স্থাপিত হয়?",
        options: ["হলদিয়া", "ডিগবয়", "মথুরা", "বরৌনি"],
        correctAnswer: 1
    },
    {
        question: "কোন শিল্পকে 'সব শিল্পের মেরুদণ্ড' বলা হয়?",
        options: ["পেট্রোকেমিক্যাল", "লোহা-ইস্পাত", "বস্ত্রবয়ন", "তথ্য প্রযুক্তি"],
        correctAnswer: 1
    },
    {
        question: "ভারতের প্রথম পাটকলটি কত সালে স্থাপিত হয়?",
        options: ["১৮৫৫", "১৮৫০", "১৮৬০", "১৮৭০"],
        correctAnswer: 0
    },
    {
        question: "ভারতের বৃহত্তম পাবলিক সেক্টর ব্যাংক কোনটি?",
        options: ["HDFC", "SBI", "PNB", "ICICI"],
        correctAnswer: 1
    },
    {
        question: "পেট্রোকেমিক্যাল শিল্পের প্রধান কাঁচামাল কী?",
        options: ["কয়লা", "ন্যাফথা", "চুনাপাথর", "আকরিক লোহা"],
        correctAnswer: 1
    },
    {
        question: "সাদা বিপ্লব (White Revolution) কোন শিল্পের সাথে যুক্ত?",
        options: ["চিনি", "দুগ্ধ", "লবণ", "তুলা"],
        correctAnswer: 1
    },
    {
        question: "ভারতের ডেকান ট্র্যাপ অঞ্চলে কোন শিল্প বেশি গড়ে উঠেছে?",
        options: ["পাট", "সুতিবস্ত্র", "চামড়া", "লোহা"],
        correctAnswer: 1
    },
    {
        question: "ভারতের প্রাচীনতম শিল্প কোনটি?",
        options: ["সুতিবস্ত্র", "পাট", "লৌহ-ইস্পাত", "চিনি"],
        correctAnswer: 0
    },
    {
        question: "হলদিয়া কোন শিল্পের জন্য বিখ্যাত?",
        options: ["সার", "পেট্রোকেমিক্যাল", "জাহাজ নির্মাণ", "সবগুলি"],
        correctAnswer: 3
    },
    {
        question: "ভারতের গ্লাস (Glass) শিল্পের প্রধান কেন্দ্র কোনটি?",
        options: ["ফিরোজাবাদ", "আগ্রা", "মুম্বাই", "কলকাতা"],
        correctAnswer: 0
    },
    {
        question: "ভারতের ডেট্রয়েট কাকে বলা হয়?",
        options: ["পুনে", "চেন্নাই", "গুরগাঁও", "ইন্দোর"],
        correctAnswer: 1
    },
    {
        question: "ভারতের শংকর ইস্পাত কেন্দ্র কোথায় অবস্থিত?",
        options: ["সালেম", "দুর্গাপুর", "ভদ্রাবতী", "সবগুলি"],
        correctAnswer: 3
    },
    {
        question: "মারুতি সুজুকি ইন্ডিয়া লিমিটেডের কারখানা কোথায় অবস্থিত?",
        options: ["গুড়গাঁও", "পুনে", "চেন্নাই", "কলকাতা"],
        correctAnswer: 0
    },
    {
        question: "ভারতের বৃহত্তম সিমেন্ট উৎপাদনকারী রাজ্য কোনটি?",
        options: ["তামিলনাড়ু", "রাজস্থান", "অন্ধ্রপ্রদেশ", "মধ্যপ্রদেশ"],
        correctAnswer: 1
    },
    {
        question: "ইন্ডিয়ান অয়েল কর্পোরেশন (IOC) কত সালে প্রতিষ্ঠিত হয়?",
        options: ["১৯৫৯", "১৯৬৪", "১৯৪৮", "১৯৭২"],
        correctAnswer: 0
    },
    {
        question: "ভারতের রেশম শিল্পের প্রধান কেন্দ্র কোনটি?",
        options: ["মহীশূর", "মুর্শিদাবাদ", "ভাগলপুর", "সবগুলি"],
        correctAnswer: 3
    },
    {
        question: "ভারতে প্রথম পশম (Woolen) বস্ত্রবয়ন শিল্প কোথায় শুরু হয়?",
        options: ["কানপুর", "লুধিয়ানা", "শ্রীনগর", "অমৃতসর"],
        correctAnswer: 0
    },
    {
        question: "ভারতের স্বর্ণতন্তু (Golden Fibre) কোন শিল্পকে বলা হয়?",
        options: ["কার্পাস", "পাট", "রেশম", "চা"],
        correctAnswer: 1
    }
];




































// gk.js - General Knowledge Quiz Logic

document.addEventListener('DOMContentLoaded', function() {

    // Quiz state variables
    let currentQuestionIndex = 0;
    let score = 0;
    let userAnswers = [];
    let quizTimer;
    let questionTimer;
    let quizStartTime;
    let quizCompleted = false;
    let autoAdvanceInterval;
    let advanceProgressInterval;

    // DOM elements
    const questionText = document.getElementById('question-text');
    const optionsContainer = document.getElementById('options-container');
    const currentQuestionElement = document.getElementById('current-question');
    const scoreElement = document.getElementById('score');
    const totalTimeElement = document.getElementById('total-time');
    const timerElement = document.getElementById('timer');
    const feedbackElement = document.getElementById('feedback');
    const resultContainer = document.getElementById('result-container');
    const finalScoreElement = document.getElementById('final-score');
    const resultMessageElement = document.getElementById('result-message');
    const correctCountElement = document.getElementById('correct-count');
    const incorrectCountElement = document.getElementById('incorrect-count');
    const timeTakenElement = document.getElementById('time-taken');
    const percentageElement = document.getElementById('percentage');
    const restartBtn = document.getElementById('restart-btn');
    const homeBtn = document.getElementById('home-btn');

    // Initialize the quiz
    function initQuiz() {
        currentQuestionIndex = 0;
        score = 0;
        userAnswers = [];
        quizCompleted = false;
        quizStartTime = Date.now();
        
        // Hide result container
        resultContainer.style.display = 'none';
        
        // Show quiz elements
        document.querySelector('.question-container').style.display = 'block';
        document.querySelector('.timer-container').style.display = 'block';
        
        // Update UI
        updateScore();
        updateQuestionCounter();
        updateTotalTime();
        
        // Load first question
        loadQuestion(currentQuestionIndex);
        
        // Start quiz timer
        startQuizTimer();
    }

    // Load a question
    function loadQuestion(index) {
        if (index >= gkQuestions.length) {
            endQuiz();
            return;
        }
        
        const question = gkQuestions[index];
        
        // Update question text
        questionText.textContent = question.question;
        
        // Clear options container
        optionsContainer.innerHTML = '';
        
        // Create option elements
        const optionLetters = ['A', 'B', 'C', 'D'];
        
        question.options.forEach((option, i) => {
            const optionElement = document.createElement('div');
            optionElement.className = 'option';
            optionElement.dataset.index = i;
            
            // Check if user has already answered this question
            if (userAnswers[index] !== undefined) {
                if (userAnswers[index] === i) {
                    optionElement.classList.add('selected');
                }
                if (i === question.correctAnswer) {
                    optionElement.classList.add('correct');
                } else if (userAnswers[index] === i && userAnswers[index] !== question.correctAnswer) {
                    optionElement.classList.add('incorrect');
                }
            }
            
            optionElement.innerHTML = `
                <div class="option-letter">${optionLetters[i]}</div>
                <div class="option-text">${option}</div>
            `;
            
            // Add click event if not already answered
            if (userAnswers[index] === undefined) {
                optionElement.addEventListener('click', () => selectOption(i));
            }
            
            optionsContainer.appendChild(optionElement);
        });
        
        // Update UI
        updateQuestionCounter();
        updateProgressBar(index + 1, gkQuestions.length);
        
        // Reset feedback
        feedbackElement.className = 'feedback';
        feedbackElement.textContent = '';
        
        // Start question timer
        startQuestionTimer();
    }

    // Select an option
    function selectOption(optionIndex) {
        // Prevent multiple selections
        if (userAnswers[currentQuestionIndex] !== undefined) return;
        
        // Mark the selected option
        const options = document.querySelectorAll('.option');
        options.forEach(option => {
            option.classList.remove('selected');
            option.style.pointerEvents = 'none'; // Disable further clicks
        });
        
        options[optionIndex].classList.add('selected');
        
        // Check answer
        const isCorrect = optionIndex === gkQuestions[currentQuestionIndex].correctAnswer;
        userAnswers[currentQuestionIndex] = optionIndex;
        
        if (isCorrect) {
            score++;
            updateScore();
            showFeedback(true);
            
            // Highlight correct answer
            options[gkQuestions[currentQuestionIndex].correctAnswer].classList.add('correct');
        } else {
            showFeedback(false, gkQuestions[currentQuestionIndex].options[gkQuestions[currentQuestionIndex].correctAnswer]);
            
            // Highlight correct and incorrect answers
            options[gkQuestions[currentQuestionIndex].correctAnswer].classList.add('correct');
            options[optionIndex].classList.add('incorrect');
        }
        
        // Stop question timer
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        // Auto advance to next question after 2 seconds
        startAutoAdvance(2000); // 2 seconds
    }

    // Auto advance to next question
    function startAutoAdvance(duration) {
        // Create or show auto-advance progress bar
        let progressBar = document.querySelector('.auto-advance-progress');
        if (!progressBar) {
            progressBar = document.createElement('div');
            progressBar.className = 'auto-advance-progress';
            progressBar.innerHTML = '<div class="advance-progress"></div>';
            feedbackElement.parentNode.insertBefore(progressBar, feedbackElement.nextSibling);
        }
        
        const progressFill = progressBar.querySelector('.advance-progress');
        progressBar.classList.add('active');
        progressFill.style.width = '0%';
        
        // Clear any existing intervals
        if (autoAdvanceInterval) clearTimeout(autoAdvanceInterval);
        if (advanceProgressInterval) clearInterval(advanceProgressInterval);
        
        // Start progress bar animation
        let progress = 0;
        const increment = 100 / (duration / 50); // Update every 50ms
        
        advanceProgressInterval = setInterval(() => {
            progress += increment;
            progressFill.style.width = `${Math.min(progress, 100)}%`;
        }, 50);
        
        // Auto advance after duration
        autoAdvanceInterval = setTimeout(() => {
            progressBar.classList.remove('active');
            clearInterval(advanceProgressInterval);
            goToNextQuestion();
        }, duration);
    }

    // Go to next question
    function goToNextQuestion() {
        currentQuestionIndex++;
        
        if (currentQuestionIndex < gkQuestions.length) {
            loadQuestion(currentQuestionIndex);
        } else {
            endQuiz();
        }
    }

    // Start question timer (30 seconds)
    function startQuestionTimer() {
        // Stop previous timer if exists
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        questionTimer = initTimer(30, onTimeUp);
        if (questionTimer) {
            questionTimer.startTimer();
        }
    }

    // Handle time up for a question
    function onTimeUp() {
        // Disable all options
        const options = document.querySelectorAll('.option');
        options.forEach(option => {
            option.style.pointerEvents = 'none';
        });
        
        // Mark the correct answer
        const correctIndex = gkQuestions[currentQuestionIndex].correctAnswer;
        options[correctIndex].classList.add('correct');
        
        // Show feedback
        showFeedback(false, gkQuestions[currentQuestionIndex].options[correctIndex]);
        
        // Auto advance to next question after 2 seconds
        startAutoAdvance(2000);
    }

    // Show feedback
    function showFeedback(isCorrect, correctAnswer = null) {
        // Update feedback message
        if (isCorrect) {
            feedbackElement.textContent = "Correct! 🎉";
            feedbackElement.className = 'feedback correct show';
            playSound('correct');
            createConfetti();
        } else {
            feedbackElement.textContent = correctAnswer ? 
                `Incorrect. Correct answer: ${correctAnswer}` : 
                "Time's up!";
            feedbackElement.className = 'feedback incorrect show';
            playSound('incorrect');
        }
    }

    // Start quiz timer (5 minutes total)
    function startQuizTimer() {
        let totalSeconds =1500; // 5 minutes
        
        const updateTimerDisplay = () => {
            totalTimeElement.textContent = formatTime(totalSeconds);
        };
        
        updateTimerDisplay();
        
        const timerInterval = setInterval(() => {
            if (quizCompleted) {
                clearInterval(timerInterval);
                return;
            }
            
            totalSeconds--;
            updateTimerDisplay();
            
            if (totalSeconds <= 0) {
                clearInterval(timerInterval);
                endQuiz();
            }
        }, 1000);
    }

    // Update question counter
    function updateQuestionCounter() {
        currentQuestionElement.textContent = `${currentQuestionIndex + 1}/${gkQuestions.length}`;
    }

    // Update score display
    function updateScore() {
        scoreElement.textContent = score;
    }

    // Update total time display
    function updateTotalTime() {
        totalTimeElement.textContent = "05:00";
    }

    // Update progress bar
    function updateProgressBar(current, total) {
        const progressBar = document.querySelector('.progress');
        if (progressBar) {
            const percentage = (current / total) * 100;
            progressBar.style.width = `${percentage}%`;
        }
    }

    // End the quiz
    function endQuiz() {
        quizCompleted = true;
        
        // Stop timers
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        // Stop auto-advance
        if (autoAdvanceInterval) clearTimeout(autoAdvanceInterval);
        if (advanceProgressInterval) clearInterval(advanceProgressInterval);
        
        // Calculate quiz duration
        const quizDuration = Math.floor((Date.now() - quizStartTime) / 1000);
        
        // Calculate results
        const correctCount = score;
        const incorrectCount = gkQuestions.length - score;
        const percentage = Math.round((score / gkQuestions.length) * 100);
        
        // Update result display
        finalScoreElement.textContent = `${score}/${gkQuestions.length}`;
        correctCountElement.textContent = correctCount;
        incorrectCountElement.textContent = incorrectCount;
        timeTakenElement.textContent = formatTime(quizDuration);
        percentageElement.textContent = `${percentage}%`;
        
        // Set result message based on performance
        let message = "";
        if (percentage >= 90) {
            message = "Outstanding! You're a General Knowledge genius! 🎉";
        } else if (percentage >= 70) {
            message = "Excellent work! You have great knowledge! 👍";
        } else if (percentage >= 50) {
            message = "Good job! You know quite a bit! 👏";
        } else {
            message = "Keep learning! You'll do better next time! 💪";
        }
        resultMessageElement.textContent = message;
        
        // Show result container with animation
        document.querySelector('.question-container').style.display = 'none';
        document.querySelector('.timer-container').style.display = 'none';
        resultContainer.style.display = 'block';
        
        // Create confetti for good scores
        if (percentage >= 70) {
            createConfetti();
        }
    }

    // Event Listeners for result buttons
    restartBtn.addEventListener('click', function() {
        initQuiz();
    });

    homeBtn.addEventListener('click', function() {
        window.location.href = "index.html";
    });

    // Initialize the quiz when page loads
    initQuiz();

});







